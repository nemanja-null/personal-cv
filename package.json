{
  "name": "cv",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Personal CV — static site built with Vite + TypeScript",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
